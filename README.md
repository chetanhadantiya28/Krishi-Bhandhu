
  # Build Frontend

  This is a code bundle for Build Frontend. The original project is available at https://www.figma.com/design/Nfp8HJzkHEz1BRbsGY35qV/Build-Frontend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  